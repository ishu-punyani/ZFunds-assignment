import { Button, Grid2, TextField } from "@mui/material";
import React from "react";

const ReviewDetails = ({ handleNextStep }: any) => {
  return (
    <Grid2 container>
    <h2>Review Details</h2>
    <Button>Submit</Button>
    </Grid2>
  );
};

export default ReviewDetails;
